apply plugin: "war"

bootWar {
    mainClassName = "id.tech.cakra.ecfgatewaysvc.EcfgatewaysvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    webAppDirName = "build/resources/main/static/"
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
